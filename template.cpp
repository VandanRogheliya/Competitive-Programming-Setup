//Link to set snippet file
//https://snippet-generator.app/

//To modify template:
//1. Press ctrl+shift+p
//2. type snippets
//3. select configure user snippets
//4. select cpp.json
//5. Go to link at the top and copy code for VS code
//6. Past it in the cpp.json, see where already exsisting snippet starts and end, past it there only

#define scani(t) scanf("%d",&t)
#define scanl(t) scanf("%ld",&t)
#define scanll(t) scanf("%lld",&t)
#define scanc(t) scanf("%c",&t)
#define scans(t) scanf("%s",t)
#define scanfl(t) scanf("%f",&t)
#define scanlfl(t) scanf("%lf",&t)

typedef map<int, int> mi;
typedef map<long long, long long> ml;
typedef set<int> si;
typedef set<long long> sl;
typedef vector<string> vs;
typedef vector<pi> vpii;
typedef priority_queue<ll> mxhpl;
typedef priority_queue<int> mxhpi;
typedef priority_queue<ll, vector<ll>, greater<ll>> mnhpl;
typedef priority_queue<int, vector<int>, greater<int>> mnhpi;


typedef long int int32;
typedef unsigned long int ul;

#define setbits(x) __builtin_popcountll(x)
#define zrobits(x) __builtin_ctzll(x)

#define INF (int)1e9

#define EPS 1e-9
#define PI 3.1415926535897932384626433832795